[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "betterkhmer"
version = "0.1.0"
description = "Khmer Unicode normalizer"
license = {text = "MIT"}
requires-python = ">=3.8"

[tool.hatch.build.targets.wheel]
packages = ["src/betterkhmer"]
